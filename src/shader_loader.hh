#pragma once
#include <FTLabel.h>
#include <GLFont.h>
#include <memory>
#include "util.hh"
 /*
class ShaderLoader
{
    shared_ptr<GLFont> font;
    unique_ptr<FTLabel> errorText;
    GLFWwindow* window;
    int fontSize;
    bool is_errored;
    
    public:

    ShaderLoader(const char* fontPath);
    ~ShaderLoader();

    GLuint load_shader(const char* fragment_shader_path, bool& reloadShader);

    void update_screen_size(int width, int height);
    void update_font_size(int newSize);
    int get_font_size() const;
}; */
